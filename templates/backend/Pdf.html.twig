<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des utilisateur</title>

    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            text-align: left;
            padding: 8px;
        }

        th {
            background-color: #6E2EBB;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2
        }
    </style>
</head>
<body>
<header>

    <h1>Liste des utilisateur</h1>
</header>

<table>
    <thead>
    <tr>
        <th>Nom</th>
        <th>Prenom</th>
        <th>Email</th>
        <th>Age</th>
        <th>Num de telephone </th>
        
    </tr>
    </thead>
    <tbody>
    {% for user in users %}
        <tr>
                <td>{{ user.nom }}</td>
                <td>{{ user.lastName }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.tel }}</td>
                <td>{{ user.age }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>