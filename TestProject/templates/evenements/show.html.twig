

{% extends 'base.html.twig' %}

{% block title %}Evenements{% endblock %}

{% block body %}
    <h1>Evenements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ evenement.nom }}</td>
            </tr>
            
          <tr>
                <th>Datedebut</th>
                <td>{{ evenement.datedebut ? evenement.datedebut|date('Y-m-d') : '' }}</td>
            </tr>

            <tr>                                  
                <th>Description</th>
                <td>{{ evenement.description }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ evenement.lieu }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ evenement.image }}</td>
            </tr>
            <tr>
                <th>Tarif</th>
                <td>{{ evenement.tarif }}</td>
            </tr>
            <tr>
                <th>PlacesDisponibles</th>
                <td>{{ evenement.placesDisponibles }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evenements_index') }}">back to list</a>

    <a href="{{ path('app_evenements_edit', {'id': evenement.id}) }}">edit</a>

    {{ include('evenements/_delete_form.html.twig') }}
{% endblock %}











