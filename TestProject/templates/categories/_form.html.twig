{{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
    <div class="form-group row">
        {{ form_label(form.nom, 'Nom', {'label_attr': {'class': 'col-sm-2 col-form-label'}}) }}
        <div class="col-sm-10">
            {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="form-group row">
        {{ form_label(form.description, 'Description', {'label_attr': {'class': 'col-sm-2 col-form-label'}}) }}
        <div class="col-sm-10">
            {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <!-- Add more fields as needed -->

   <div class="card-footer text-center">
    <button type="submit" class="btn btn-primary">
        <i class="zmdi zmdi-plus"></i> {{ button_label|default('Ajouter') }}
    </button>
    <a href="{{ path('app_categories_index') }}" class="btn btn-primary">
        Consulter Liste
    </a>
</div>

{{ form_end(form) }}
