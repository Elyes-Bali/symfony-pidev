<form method="post" action="{{ path('app_destination_delete', {'iddest': destination.iddest}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ destination.iddest) }}">
    <button class="btn">Delete</button>
</form>
