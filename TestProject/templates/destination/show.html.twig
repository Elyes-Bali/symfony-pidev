{% extends 'base.html.twig' %}

{% block title %}Destination{% endblock %}

{% block body %}
    <h1>Destination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Iddest</th>
                <td>{{ destination.iddest }}</td>
            </tr>
            <tr>
                <th>Countries</th>
                <td>{{ destination.countries }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_destination_index') }}">back to list</a>

    <a href="{{ path('app_destination_edit', {'iddest': destination.iddest}) }}">edit</a>

    {{ include('destination/_delete_form.html.twig') }}
{% endblock %}
