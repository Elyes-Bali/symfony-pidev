<form method="post" action="{{ path('app_reponsereclamation_delete', {'idreprec': reponsereclamation.idreprec}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponsereclamation.idreprec) }}">
    <button class="btn">Delete</button>
</form>
