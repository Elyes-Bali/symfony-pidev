{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdComent</th>
                <td>{{ commentaire.idComent }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ commentaire.nom }}</td>
            </tr>
            <tr>
                <th>Datenow</th>
                <td>{{ commentaire.datenow ? commentaire.datenow|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit', {'idComent': commentaire.idComent}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
